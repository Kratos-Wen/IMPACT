# Model Default Configs

Training loads defaults from `configs/<model_name>.json` before CLI parsing.

- `scalant` / `sca` -> `configs/sca.json`

CLI arguments still take precedence over these defaults.
