# AF-L Assets

This directory reserves the `AF-L` protocol slot in the public IMPACT repository.

Current status:
- runnable `AF-L` protocol assets are not released yet
- the directory is kept so the dataset layout matches the paper taxonomy

When the long-horizon forecasting release is added, this directory will host the lightweight benchmark assets required by `tasks/AF-L/`.
